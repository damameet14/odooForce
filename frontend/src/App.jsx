import { useEffect, useState } from "react";
import { Navigate, Route, Routes, NavLink, useLocation, useNavigate, useParams } from "react-router-dom";
import { BarChart3, Bell, Building2, Check, ChevronRight, CircleDollarSign, ClipboardCheck, FileText, LayoutDashboard, LogOut, Menu, PackageCheck, Plus, Search, Send, ShoppingCart, Trash2, Users, X } from "lucide-react";
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { toast } from "sonner";
import api from "./api/client";
import { useAuth } from "./store/auth.jsx";

const money = (v) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(Number(v || 0));
const date = (v) => v ? new Intl.DateTimeFormat("en-IN", { dateStyle: "medium" }).format(new Date(v)) : "-";
const titleCase = (v = "") => v.replaceAll("_", " ").toLowerCase().replace(/\b\w/g, (c) => c.toUpperCase());
const roles = { ADMIN: "Manager", PROCUREMENT_OFFICER: "Procurement Officer", FINANCE_OFFICER: "Finance Officer", VENDOR: "Vendor" };
const employeeRoles = ["ADMIN", "PROCUREMENT_OFFICER"];
const statusStyle = {
  APPROVED: "green", DELIVERED: "green", COMPLETED: "green", ACTIVE: "green", PAID: "green",
  REJECTED: "red", CANCELLED: "red", BLACKLISTED: "red", OVERDUE: "red",
  PENDING: "amber", APPROVAL_PENDING: "amber", UNPAID: "amber", PARTIALLY_PAID: "amber",
  SENT: "blue", SUBMITTED: "blue", GENERATED: "blue", DISPATCHED: "blue", PO_GENERATED: "teal",
  DRAFT: "gray", INACTIVE: "gray", CLOSED: "gray", UNDER_REVIEW: "violet", SELECTED: "violet",
};
function Status({ value }) { return <span className={`status ${statusStyle[value] || "gray"}`}><i />{titleCase(value)}</span>; }
function Button({ children, icon: Icon, secondary, danger, ...props }) { return <button className={`button ${secondary ? "secondary" : ""} ${danger ? "danger" : ""}`} {...props}>{Icon && <Icon size={15} />}{children}</button>; }
function ErrorBox({ error }) { return error ? <div className="error">{error.response?.data?.message || error.message}</div> : null; }
function Empty({ text = "No records found" }) { return <div className="empty"><Search size={26} /><p>{text}</p></div>; }
function PageTitle({ title, subtitle, action }) { return <div className="page-title"><div><h1>{title}</h1><p>{subtitle}</p></div>{action}</div>; }
function Table({ headers, children, empty }) { return <div className="table-wrap"><table><thead><tr>{headers.map((h) => <th key={h}>{h}</th>)}</tr></thead><tbody>{children}</tbody></table>{empty && <Empty />}</div>; }

function useLoad(path, deps = []) {
  const [data, setData] = useState(null); const [error, setError] = useState(null); const [tick, setTick] = useState(0);
  useEffect(() => { let mounted = true; setError(null); api.get(path).then((r) => mounted && setData(r.data)).catch((e) => mounted && setError(e)); return () => { mounted = false; }; }, [path, tick, ...deps]);
  return { data, error, reload: () => setTick((v) => v + 1) };
}
function useList(path, deps = []) {
  const result = useLoad(path, deps);
  return { ...result, data: Array.isArray(result.data) ? result.data : [] };
}
async function action(method, path, payload, done) {
  try { const { data } = await api[method](path, payload); toast.success("Action completed"); done?.(data); return data; }
  catch (e) { toast.error(e.response?.data?.message || e.message); }
}

const menu = {
  ADMIN: [[LayoutDashboard, "Dashboard", "/dashboard"], [Users, "Employees", "/users"], [Building2, "Vendors", "/vendors"], [ShoppingCart, "RFQs", "/rfqs"], [PackageCheck, "Purchase orders", "/purchase-orders"], [FileText, "Invoices", "/invoices"], [BarChart3, "Reports", "/reports"], [ClipboardCheck, "Activity logs", "/activity-logs"]],
  PROCUREMENT_OFFICER: [[LayoutDashboard, "Dashboard", "/dashboard"], [ShoppingCart, "RFQs", "/rfqs"], [ClipboardCheck, "Approvals", "/approvals"], [PackageCheck, "Purchase orders", "/purchase-orders"], [FileText, "Invoices", "/invoices"], [ClipboardCheck, "Activity logs", "/activity-logs"]],
  FINANCE_OFFICER: [[LayoutDashboard, "Dashboard", "/dashboard"], [ClipboardCheck, "Approvals", "/approvals"], [PackageCheck, "Purchase orders", "/purchase-orders"], [FileText, "Invoices", "/invoices"], [BarChart3, "Reports", "/reports"]],
  VENDOR: [[LayoutDashboard, "Dashboard", "/dashboard"], [ShoppingCart, "Assigned RFQs", "/rfqs"], [PackageCheck, "Purchase orders", "/purchase-orders"], [FileText, "Invoices", "/invoices"]],
};
function Shell() {
  const { user, logout } = useAuth(); const [open, setOpen] = useState(false); const location = useLocation();
  return <div className="shell">
    <aside className={open ? "open" : ""}>
      <div className="brand"><span>VB</span><div><strong>VendorBridge</strong><small>Procurement ERP</small></div><button className="mobile-close" onClick={() => setOpen(false)}><X size={18} /></button></div>
      <nav>{menu[user.role].map(([Icon, label, path]) => <NavLink to={path} key={path} onClick={() => setOpen(false)} className={({ isActive }) => isActive || location.pathname.startsWith(`${path}/`) ? "active" : ""}><Icon size={17} />{label}<ChevronRight size={14} /></NavLink>)}</nav>
      <div className="side-user"><div className="avatar">{user.name.slice(0, 2).toUpperCase()}</div><div><strong>{user.name}</strong><small>{roles[user.role]}</small></div><button title="Log out" onClick={logout}><LogOut size={17} /></button></div>
    </aside>
    <main><header><button className="menu-button" onClick={() => setOpen(true)}><Menu size={20} /></button><div className="context">Workspace <ChevronRight size={13} /> <strong>{roles[user.role]}</strong></div><NavLink to="/notifications" className="icon-button"><Bell size={18} /></NavLink></header><div className="content"><AppRoutes /></div></main>
  </div>;
}

function Login() {
  const { login } = useAuth(); const nav = useNavigate(); const [error, setError] = useState(); const [loading, setLoading] = useState(false);
  const submit = async (e) => { e.preventDefault(); setLoading(true); setError(); const form = new FormData(e.currentTarget); try { await login(form.get("email"), form.get("password")); nav("/dashboard"); } catch (err) { setError(err); } finally { setLoading(false); } };
  return <div className="login"><section><div className="login-brand"><span>VB</span><strong>VendorBridge</strong></div><div className="login-copy"><small>PROCUREMENT CONTROL CENTER</small><h1>One clear path from request to delivery.</h1><p>Manage vendor sourcing, approvals, purchase orders, invoices, and fulfillment in a single operational workspace.</p><div className="flow"><span>RFQ</span><i /><span>Quote</span><i /><span>Approve</span><i /><span>Deliver</span></div></div><footer>Secure role-based procurement operations</footer></section><form onSubmit={submit}><div><h2>Sign in</h2><p>Use your assigned account to enter your workspace.</p><ErrorBox error={error} /><label>Email<input name="email" type="email" required /></label><label>Password<input name="password" type="password" required /></label><Button type="submit" disabled={loading}>{loading ? "Signing in..." : "Sign in"}</Button></div></form></div>;
}

function Dashboard() {
  const { user } = useAuth(); const { data, error } = useLoad("/reports/dashboard-summary"); const spend = useLoad("/reports/monthly-spend");
  const cards = user.role === "VENDOR" ? [["Assigned RFQs", data?.rfqs, ShoppingCart], ["Quotations", data?.quotations, ClipboardCheck], ["Purchase orders", data?.purchaseOrders, PackageCheck], ["Invoices", data?.invoices, FileText]]
    : [["Active RFQs", data?.rfqs, ShoppingCart], ["Pending approvals", data?.approvals, ClipboardCheck], ["Purchase orders", data?.purchaseOrders, PackageCheck], ["Procurement value", money(data?.totalSpend), CircleDollarSign]];
  return <><PageTitle title={`Good day, ${user.name.split(" ")[0]}`} subtitle="Here is the current state of your procurement workflow." action={user.role === "PROCUREMENT_OFFICER" && <Button icon={Plus} onClick={() => location.href = "/rfqs/create"}>Create RFQ</Button>} /><ErrorBox error={error} /><div className="stats">{cards.map(([label, value, Icon]) => <div className="stat" key={label}><div><span>{label}</span><strong>{value ?? "-"}</strong></div><Icon size={20} /></div>)}</div><div className="dashboard-grid"><section className="panel chart"><div className="panel-head"><div><h2>Monthly procurement spend</h2><p>Approved purchase order value</p></div></div><ResponsiveContainer width="100%" height={260}><BarChart data={spend.data || []}><CartesianGrid strokeDasharray="3 3" vertical={false} /><XAxis dataKey="month" /><YAxis tickFormatter={(v) => `${v / 1000}k`} /><Tooltip formatter={money} /><Bar dataKey="total" fill="#167d71" radius={[4, 4, 0, 0]} /></BarChart></ResponsiveContainer></section><QuickActions role={user.role} /></div></>;
}
function QuickActions({ role }) {
  const nav = useNavigate(); const options = role === "PROCUREMENT_OFFICER" ? [["Create RFQ", "/rfqs/create"], ["Review approvals", "/approvals"], ["View purchase orders", "/purchase-orders"]]
    : role === "FINANCE_OFFICER" ? [["Review approvals", "/approvals"], ["View reports", "/reports"], ["Track invoices", "/invoices"]]
      : role === "ADMIN" ? [["Manage employees", "/users"], ["Manage vendors", "/vendors"], ["Review activity", "/activity-logs"]] : [["View assigned RFQs", "/rfqs"], ["Track deliveries", "/purchase-orders"], ["View invoices", "/invoices"]];
  return <section className="panel"><div className="panel-head"><div><h2>Quick actions</h2><p>Common tasks for your role</p></div></div><div className="quick">{options.map(([label, path]) => <button key={path} onClick={() => nav(path)}><span>{label}</span><ChevronRight size={16} /></button>)}</div></section>;
}

function RfqList() {
  const { user } = useAuth(); const { data, error } = useList("/rfqs"); const nav = useNavigate();
  return <><PageTitle title={user.role === "VENDOR" ? "Assigned RFQs" : "Requests for quotation"} subtitle="Track sourcing requests and vendor participation." action={user.role === "PROCUREMENT_OFFICER" && <Button icon={Plus} onClick={() => nav("/rfqs/create")}>Create RFQ</Button>} /><ErrorBox error={error} /><Table headers={["RFQ", "Title", "Deadline", "Items", "Vendors", "Quotes", "Status", ""]} empty={!data.length}>{data.map((r) => <tr key={r.id}><td className="mono">{r.rfqNumber}</td><td><strong>{r.title}</strong><small>{r.creator?.name}</small></td><td>{date(r.deadline)}</td><td>{r._count?.items ?? "-"}</td><td>{r._count?.invites ?? "-"}</td><td>{r._count?.quotations ?? "-"}</td><td><Status value={r.status} /></td><td><Button secondary onClick={() => nav(`/rfqs/${r.id}`)}>Open</Button></td></tr>)}</Table></>;
}
function CreateRfq() {
  const nav = useNavigate(); const vendors = useLoad("/vendors"); const [items, setItems] = useState([{ itemName: "", quantity: 1, unit: "pieces", specifications: "" }]); const [error, setError] = useState();
  const submit = async (e) => { e.preventDefault(); const form = new FormData(e.currentTarget); try { const { data } = await api.post("/rfqs", { title: form.get("title"), description: form.get("description"), deadline: form.get("deadline"), expectedDeliveryDate: form.get("expectedDeliveryDate") || null, vendorIds: form.getAll("vendorIds"), items }); toast.success("RFQ created"); nav(`/rfqs/${data.id}`); } catch (err) { setError(err); } };
  return <><PageTitle title="Create RFQ" subtitle="Define the requirement, deadline, items, and invited vendors." /><form className="form-layout" onSubmit={submit}><section className="panel form-section"><h2>Request details</h2><ErrorBox error={error} /><div className="fields"><label>Title<input name="title" required placeholder="Production material requirement" /></label><label>Quotation deadline<input name="deadline" type="datetime-local" required /></label><label>Expected delivery<input name="expectedDeliveryDate" type="date" /></label><label className="full">Description<textarea name="description" rows="3" placeholder="Describe the procurement need" /></label></div></section><section className="panel form-section"><div className="panel-head"><h2>Line items</h2><Button type="button" secondary icon={Plus} onClick={() => setItems([...items, { itemName: "", quantity: 1, unit: "pieces", specifications: "" }])}>Add item</Button></div>{items.map((item, i) => <div className="line-item" key={i}><label>Item name<input value={item.itemName} onChange={(e) => setItems(items.map((x, n) => n === i ? { ...x, itemName: e.target.value } : x))} required /></label><label>Quantity<input type="number" min="0.01" step="0.01" value={item.quantity} onChange={(e) => setItems(items.map((x, n) => n === i ? { ...x, quantity: Number(e.target.value) } : x))} required /></label><label>Unit<input value={item.unit} onChange={(e) => setItems(items.map((x, n) => n === i ? { ...x, unit: e.target.value } : x))} required /></label><label>Specifications<input value={item.specifications} onChange={(e) => setItems(items.map((x, n) => n === i ? { ...x, specifications: e.target.value } : x))} /></label>{items.length > 1 && <button type="button" onClick={() => setItems(items.filter((_, n) => n !== i))}><X size={17} /></button>}</div>)}</section><section className="panel form-section"><h2>Invite vendors</h2><div className="vendor-options">{vendors.data?.map((v) => <label key={v.id}><input type="checkbox" name="vendorIds" value={v.id} /><span><strong>{v.companyName}</strong><small>{v.category?.name || "Uncategorized"} · Rating {v.rating}</small></span></label>)}</div></section><div className="form-actions"><Button type="button" secondary onClick={() => nav("/rfqs")}>Cancel</Button><Button type="submit">Create draft RFQ</Button></div></form></>;
}
function RfqDetail() {
  const { id } = useParams(); const { user } = useAuth(); const { data: r, error, reload } = useLoad(`/rfqs/${id}`); const nav = useNavigate();
  if (!r) return <ErrorBox error={error} />;
  return <><PageTitle title={r.rfqNumber} subtitle={r.title} action={<div className="actions">{user.role === "PROCUREMENT_OFFICER" && r.status === "DRAFT" && <Button icon={Send} onClick={() => action("post", `/rfqs/${id}/send`, {}, reload)}>Send RFQ</Button>}{user.role === "PROCUREMENT_OFFICER" && ["QUOTATIONS_RECEIVED", "UNDER_REVIEW"].includes(r.status) && <Button onClick={() => nav(`/rfqs/${id}/compare`)}>Compare quotations</Button>}{user.role === "VENDOR" && ["SENT", "QUOTATIONS_RECEIVED"].includes(r.status) && <Button onClick={() => nav(`/rfqs/${id}/quote`)}>Submit quotation</Button>}</div>} /><div className="detail-grid"><section className="panel"><div className="detail-head"><Status value={r.status} /><span>Deadline {date(r.deadline)}</span></div><p>{r.description}</p><h3>Requested items</h3><div className="items">{r.items.map((i) => <div key={i.id}><strong>{i.itemName}</strong><span>{i.quantity} {i.unit}</span><small>{i.specifications}</small></div>)}</div></section><section className="panel"><h2>Invited vendors</h2><div className="list">{r.invites.map((i) => <div key={i.id}><div><strong>{i.vendor.companyName}</strong><small>{i.vendor.category?.name || i.vendor.email}</small></div><Status value={i.status} /></div>)}</div></section></div></>;
}
function QuoteForm() {
  const { id } = useParams(); const { data: r, error } = useLoad(`/rfqs/${id}`); const nav = useNavigate(); const [prices, setPrices] = useState({});
  if (!r) return <ErrorBox error={error} />;
  const submit = async (e) => { e.preventDefault(); const f = new FormData(e.currentTarget); const items = r.items.map((i) => ({ rfqItemId: i.id, quantity: Number(i.quantity), unitPrice: Number(prices[i.id]?.price || 0), taxPercentage: Number(prices[i.id]?.tax || 0), discount: 0 })); const result = await action("post", "/quotations", { rfqId: id, deliveryTimeline: f.get("deliveryTimeline"), paymentTerms: f.get("paymentTerms"), notes: f.get("notes"), items }, () => nav("/rfqs")); return result; };
  return <><PageTitle title="Submit quotation" subtitle={`${r.rfqNumber} · ${r.title}`} /><form className="form-layout" onSubmit={submit}><section className="panel form-section"><h2>Item pricing</h2>{r.items.map((i) => <div className="quote-line" key={i.id}><div><strong>{i.itemName}</strong><small>{i.quantity} {i.unit}</small></div><label>Unit price<input type="number" min="0" step="0.01" required onChange={(e) => setPrices({ ...prices, [i.id]: { ...prices[i.id], price: e.target.value } })} /></label><label>Tax %<input type="number" min="0" step="0.01" defaultValue="18" onChange={(e) => setPrices({ ...prices, [i.id]: { ...prices[i.id], tax: e.target.value } })} /></label></div>)}</section><section className="panel form-section"><h2>Commercial terms</h2><div className="fields"><label>Delivery timeline<input name="deliveryTimeline" required placeholder="12 days" /></label><label>Payment terms<input name="paymentTerms" placeholder="Net 30" /></label><label className="full">Notes<textarea name="notes" rows="3" /></label></div></section><div className="form-actions"><Button secondary type="button" onClick={() => nav(-1)}>Cancel</Button><Button type="submit">Submit quotation</Button></div></form></>;
}
function Compare() {
  const { id } = useParams(); const { data, error } = useList(`/rfqs/${id}/quotations/compare`); const nav = useNavigate();
  return <><PageTitle title="Quotation comparison" subtitle="Ranked by price, delivery timeline, and vendor rating." /><ErrorBox error={error} /><div className="compare">{data.map((q) => <section className="panel" key={q.id}><div className="compare-head"><div><small>{q.quotationNumber}</small><h2>{q.vendor.companyName}</h2><span>Rating {q.vendor.rating} · {q.deliveryTimeline}</span></div><strong>{q.score}<small>/100</small></strong></div><div className="badges">{q.badges.map((b) => <span key={b}>{b}</span>)}</div><div className="total"><span>Grand total</span><strong>{money(q.grandTotal)}</strong></div><div className="mini-items">{q.items.map((i) => <div key={i.id}><span>{i.rfqItem.itemName}</span><strong>{money(i.totalAmount)}</strong></div>)}</div><Button onClick={() => action("post", `/rfqs/${id}/quotations/${q.id}/select`, {}, () => nav("/approvals"))}>Recommend for approval</Button></section>)}</div>{!data.length && <Empty text="No quotations submitted yet" />}</>;
}

function Approvals() {
  const { user } = useAuth(); const { data, error, reload } = useList("/approvals");
  return <><PageTitle title="Approval requests" subtitle="Finance review queue and procurement decisions." /><ErrorBox error={error} /><Table headers={["RFQ", "Selected vendor", "Amount", "Requested by", "Status", "Remarks", "Actions"]} empty={!data.length}>{data.map((a) => <tr key={a.id}><td><strong>{a.rfq.rfqNumber}</strong><small>{a.rfq.title}</small></td><td>{a.selectedQuotation.vendor.companyName}</td><td>{money(a.selectedQuotation.grandTotal)}</td><td>{a.requester.name}</td><td><Status value={a.status} /></td><td>{a.remarks || "-"}</td><td><div className="actions">{user.role === "FINANCE_OFFICER" && a.status === "PENDING" && <><Button icon={Check} onClick={() => action("put", `/approvals/${a.id}/approve`, { remarks: "Approved" }, reload)}>Approve</Button><Button danger icon={X} onClick={() => action("put", `/approvals/${a.id}/reject`, { remarks: "Rejected by finance" }, reload)}>Reject</Button></>}{user.role === "PROCUREMENT_OFFICER" && a.status === "APPROVED" && <Button onClick={() => action("post", "/purchase-orders/generate", { approvalId: a.id }, reload)}>Generate PO</Button>}</div></td></tr>)}</Table></>;
}
function PurchaseOrders() {
  const { user } = useAuth(); const { data, error, reload } = useList("/purchase-orders");
  const download = async (id) => { const r = await api.get(`/purchase-orders/${id}/pdf`, { responseType: "blob" }); window.open(URL.createObjectURL(r.data)); };
  return <><PageTitle title="Purchase orders" subtitle="Approved commitments and delivery progress." /><ErrorBox error={error} /><Table headers={["PO number", "Vendor", "RFQ", "Value", "Created", "Status", "Actions"]} empty={!data.length}>{data.map((p) => <tr key={p.id}><td className="mono">{p.poNumber}</td><td>{p.vendor.companyName}</td><td>{p.rfq.rfqNumber}</td><td>{money(p.grandTotal)}</td><td>{date(p.createdAt)}</td><td><Status value={p.status} /></td><td><div className="actions"><Button secondary onClick={() => download(p.id)}>PDF</Button>{user.role === "VENDOR" && p.status !== "DELIVERED" && <Button onClick={() => action("put", `/purchase-orders/${p.id}/status`, { status: p.status === "GENERATED" ? "ACKNOWLEDGED" : p.status === "ACKNOWLEDGED" ? "READY" : p.status === "READY" ? "DISPATCHED" : "DELIVERED" }, reload)}>Advance status</Button>}{user.role === "PROCUREMENT_OFFICER" && !p.invoices?.length && <Button onClick={() => action("post", "/invoices/generate", { purchaseOrderId: p.id }, reload)}>Generate invoice</Button>}</div></td></tr>)}</Table></>;
}
function Invoices() {
  const { user } = useAuth(); const { data, error, reload } = useList("/invoices");
  return <><PageTitle title="Invoices" subtitle="Generated documents and payment tracking." /><ErrorBox error={error} /><Table headers={["Invoice", "Vendor", "PO", "Due date", "Value", "Payment", "Actions"]} empty={!data.length}>{data.map((i) => <tr key={i.id}><td className="mono">{i.invoiceNumber}</td><td>{i.vendor.companyName}</td><td className="mono">{i.purchaseOrder.poNumber}</td><td>{date(i.dueDate)}</td><td>{money(i.grandTotal)}</td><td><Status value={i.paymentStatus} /></td><td><div className="actions"><Button secondary onClick={async () => { const r = await api.get(`/invoices/${i.id}/pdf`, { responseType: "blob" }); window.open(URL.createObjectURL(r.data)); }}>PDF</Button>{user.role === "FINANCE_OFFICER" && i.paymentStatus !== "PAID" && <Button onClick={() => action("put", `/invoices/${i.id}/payment-status`, { paymentStatus: "PAID" }, reload)}>Mark paid</Button>}</div></td></tr>)}</Table></>;
}

function Vendors() {
  const { data, error, reload } = useList("/vendors"); const categories = useList("/vendor-categories"); const [show, setShow] = useState(false); const [showCategory, setShowCategory] = useState(false);
  const create = async (e) => { e.preventDefault(); const f = Object.fromEntries(new FormData(e.currentTarget)); await action("post", "/vendors", f, () => { setShow(false); reload(); }); };
  const createCategory = async (e) => { e.preventDefault(); await action("post", "/vendor-categories", Object.fromEntries(new FormData(e.currentTarget)), () => { setShowCategory(false); categories.reload(); }); };
  return <><PageTitle title="Vendors" subtitle="Supplier master data, category, rating, and status." action={<div className="actions"><Button secondary icon={Plus} onClick={() => setShowCategory(true)}>Add category</Button><Button icon={Plus} onClick={() => setShow(true)}>Add vendor</Button></div>} /><ErrorBox error={error || categories.error} />{showCategory && <section className="panel inline-form"><div className="panel-head"><h2>Add category</h2><button onClick={() => setShowCategory(false)}><X size={18} /></button></div><form onSubmit={createCategory} className="fields"><label>Category name<input name="name" required maxLength="100" placeholder="Packaging" /></label><label className="full">Description<textarea name="description" rows="2" maxLength="500" placeholder="Optional category notes" /></label><div className="form-actions full"><Button type="submit">Create category</Button></div></form></section>}{show && <section className="panel inline-form"><div className="panel-head"><h2>Add vendor</h2><button onClick={() => setShow(false)}><X size={18} /></button></div><form onSubmit={create} className="fields"><label>Company name<input name="companyName" required maxLength="160" /></label><label>Category<select name="categoryId" defaultValue="">{<option value="">Uncategorized</option>}{categories.data.map((c) => <option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>Contact person<input name="contactPerson" maxLength="120" /></label><label>Email<input name="email" type="email" required /></label><label>Phone<input name="phone" type="tel" maxLength="30" /></label><label>GST number<input name="gstNumber" maxLength="40" /></label><label>Rating<input name="rating" type="number" min="0" max="5" step="0.1" defaultValue="0" /></label><label>Status<select name="status" defaultValue="ACTIVE">{["ACTIVE", "PENDING", "INACTIVE", "BLACKLISTED"].map((s) => <option key={s}>{s}</option>)}</select></label><label>Initial password<input name="password" type="password" minLength="8" maxLength="128" required /></label><div className="form-actions"><Button type="submit">Create vendor</Button></div></form></section>}<Table headers={["Company", "Category", "Contact", "GST", "Rating", "Status"]} empty={!data.length}>{data.map((v) => <tr key={v.id}><td><strong>{v.companyName}</strong><small>{v.email}</small></td><td>{v.category?.name || "-"}</td><td>{v.contactPerson || "-"}<small>{v.phone}</small></td><td>{v.gstNumber || "-"}</td><td>{v.rating} / 5</td><td><Status value={v.status} /></td></tr>)}</Table></>;
}
function EmployeesPage() {
  const { data, error, reload } = useList("/users"); const [show, setShow] = useState(false); const [selected, setSelected] = useState(null);
  useEffect(() => {
    if (!selected) return;
    const fresh = data.find((user) => user.id === selected.id);
    if (fresh) setSelected(fresh);
  }, [data]);
  const create = async (e) => { e.preventDefault(); await action("post", "/users", Object.fromEntries(new FormData(e.currentTarget)), () => { setShow(false); reload(); }); };
  const update = async (e) => {
    e.preventDefault();
    const payload = Object.fromEntries(new FormData(e.currentTarget));
    if (!payload.password) delete payload.password;
    const updated = await action("put", `/users/${selected.id}`, payload);
    if (updated) {
      setSelected(null);
      reload();
    }
  };
  const remove = async () => {
    if (!selected || !confirm(`Deactivate ${selected.name}?`)) return;
    const updated = await action("delete", `/users/${selected.id}`, {});
    if (updated) {
      setSelected((current) => ({ ...current, ...updated }));
      reload();
    }
  };
  return <><PageTitle title="Employees" subtitle="Internal account access for managers and procurement officers." action={<Button icon={Plus} onClick={() => { setShow(true); setSelected(null); }}>Add employee</Button>} /><ErrorBox error={error} />{show && <section className="panel inline-form"><div className="panel-head"><h2>Add employee</h2><button onClick={() => setShow(false)}><X size={18} /></button></div><form onSubmit={create} className="fields"><label>Name<input name="name" required maxLength="120" /></label><label>Email<input name="email" type="email" required /></label><label>Phone<input name="phone" type="tel" inputMode="numeric" pattern="[0-9]{10}" minLength="10" maxLength="10" title="Enter a 10-digit phone number" required /></label><label>Role<select name="role" required>{employeeRoles.map((r) => <option key={r} value={r}>{roles[r]}</option>)}</select></label><label>Status<select name="status" defaultValue="ACTIVE" required>{["ACTIVE", "INACTIVE"].map((s) => <option key={s}>{s}</option>)}</select></label><label>Password<input name="password" type="password" minLength="8" maxLength="128" required /></label><div className="form-actions"><Button type="submit">Create employee</Button></div></form></section>}{selected && <section className="panel inline-form"><div className="panel-head"><div><h2>Edit employee</h2><p>{selected.email}</p></div><button onClick={() => setSelected(null)}><X size={18} /></button></div><form key={selected.id} onSubmit={update} className="fields"><label>Name<input name="name" defaultValue={selected.name} required maxLength="120" /></label><label>Email<input name="email" type="email" defaultValue={selected.email} required /></label><label>Phone<input name="phone" type="tel" inputMode="numeric" pattern="[0-9]{10}" minLength="10" maxLength="10" title="Enter a 10-digit phone number" defaultValue={selected.phone || ""} required /></label><label>Role<select name="role" defaultValue={employeeRoles.includes(selected.role) ? selected.role : "PROCUREMENT_OFFICER"} required>{employeeRoles.map((r) => <option key={r} value={r}>{roles[r]}</option>)}</select></label><label>Status<select name="status" defaultValue={selected.status} required>{["ACTIVE", "INACTIVE"].map((s) => <option key={s}>{s}</option>)}</select></label><label>New password<input name="password" type="password" minLength="8" maxLength="128" placeholder="Leave blank to keep current" /></label><div className="form-actions full"><Button type="button" danger icon={Trash2} onClick={remove}>Deactivate employee</Button><Button type="submit">Save changes</Button></div></form></section>}<Table headers={["Name", "Email", "Role", "Status", "Created", "Actions"]} empty={!data.length}>{data.map((u) => <tr key={u.id} className={`user-row ${selected?.id === u.id ? "selected-row" : ""}`} onClick={() => { setSelected(u); setShow(false); }}><td><strong>{u.name}</strong><small>Click to edit</small></td><td>{u.email}</td><td>{roles[u.role]}</td><td><Status value={u.status} /></td><td>{date(u.createdAt)}</td><td><Button secondary type="button" onClick={(e) => { e.stopPropagation(); setSelected(u); setShow(false); }}>Edit</Button></td></tr>)}</Table></>;
}
function Reports() {
  const vendors = useLoad("/reports/vendor-performance"); const rfqs = useLoad("/reports/rfq-summary");
  return <><PageTitle title="Reports & analytics" subtitle="Procurement health, supplier performance, and export." action={<Button onClick={async () => { const r = await api.get("/reports/export/procurement-summary", { responseType: "blob" }); const a = document.createElement("a"); a.href = URL.createObjectURL(r.data); a.download = "procurement-summary.csv"; a.click(); }}>Export CSV</Button>} /><div className="detail-grid"><section className="panel"><h2>Vendor performance</h2><div className="list">{vendors.data?.map((v) => <div key={v.id}><div><strong>{v.companyName}</strong><small>{v._count.purchaseOrders} purchase orders · {v._count.quotations} quotes</small></div><strong>{v.rating}/5</strong></div>)}</div></section><section className="panel"><h2>RFQ status summary</h2><div className="list">{rfqs.data?.map((r) => <div key={r.status}><Status value={r.status} /><strong>{r._count.status}</strong></div>)}</div></section></div></>;
}
function Activity() { const { data, error } = useList("/activity-logs"); return <><PageTitle title="Activity logs" subtitle="Audit trail for major procurement actions." /><ErrorBox error={error} /><Table headers={["Action", "Description", "User", "Entity", "Time"]} empty={!data.length}>{data.map((l) => <tr key={l.id}><td className="mono">{l.action}</td><td>{l.description}</td><td>{l.user?.name || "System"}</td><td>{titleCase(l.entityType)}</td><td>{date(l.createdAt)}</td></tr>)}</Table></>; }
function Notifications() { const { data, error, reload } = useList("/notifications"); return <><PageTitle title="Notifications" subtitle="Updates relevant to your role and workflow." action={<Button secondary onClick={() => action("put", "/notifications/read-all", {}, reload)}>Mark all read</Button>} /><ErrorBox error={error} /><div className="notifications">{data.map((n) => <button className={n.isRead ? "" : "unread"} key={n.id} onClick={() => action("put", `/notifications/${n.id}/read`, {}, reload)}><Bell size={17} /><div><strong>{n.title}</strong><p>{n.message}</p><small>{date(n.createdAt)}</small></div></button>)}</div>{!data.length && <Empty />}</>; }

function AppRoutes() {
  const { user } = useAuth();
  return <Routes><Route path="/dashboard" element={<Dashboard />} /><Route path="/rfqs" element={<RfqList />} /><Route path="/rfqs/create" element={user.role === "PROCUREMENT_OFFICER" ? <CreateRfq /> : <Navigate to="/rfqs" />} /><Route path="/rfqs/:id" element={<RfqDetail />} /><Route path="/rfqs/:id/quote" element={user.role === "VENDOR" ? <QuoteForm /> : <Navigate to="/rfqs" />} /><Route path="/rfqs/:id/compare" element={<Compare />} /><Route path="/approvals" element={<Approvals />} /><Route path="/purchase-orders" element={<PurchaseOrders />} /><Route path="/invoices" element={<Invoices />} /><Route path="/vendors" element={<Vendors />} /><Route path="/users" element={<EmployeesPage />} /><Route path="/reports" element={<Reports />} /><Route path="/activity-logs" element={<Activity />} /><Route path="/notifications" element={<Notifications />} /><Route path="*" element={<Navigate to="/dashboard" />} /></Routes>;
}
export default function App() { const { user } = useAuth(); return user ? <Shell /> : <Routes><Route path="*" element={<Login />} /></Routes>; }
